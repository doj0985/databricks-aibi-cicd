# Databricks bundle local state, terraform, and cached auth
.databricks/

# Python
build/
dist/
__pycache__/
*.egg-info
.venv/

# OS noise
.DS_Store

# Local drafts (not part of the framework)
BLOG.md

# Local secrets / env files — NEVER commit credentials
.env
*.env
.env.local

# Scratch workspace (DAB template convention — keep the README)
scratch/**
!scratch/README.md
